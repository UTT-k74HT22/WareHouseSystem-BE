spring:
  application:
    name: ${SPRING_APPLICATION_NAME:whs-backend}

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 100MB
      file-size-threshold: 2MB

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

  session:
    store-type: redis
    timeout: 1800s
    redis:
      flush-mode: on_save
      namespace: spring:session

  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML
    cache: false

server:
  port: ${SERVER_PORT:8080}
  servlet:
    context-path: /
    session:
      timeout: 30m

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when_authorized
      probes:
        enabled: true

app:
  jwt:
    secret: ${JWT_SECRET}
    access-token-expiration: 3600s
    refresh-token-expiration: 86400s
    reset-token-expiration: PT5M

  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:4200,http://localhost:5173,http://localhost:8080}
    allowed-methods: ${CORS_ALLOWED_METHODS:GET,POST,PUT,PATCH,DELETE,OPTIONS}
    allowed-headers: ${CORS_ALLOWED_HEADERS:*}
    max-age: ${CORS_MAX_AGE:3600}

  rate-limit:
    enabled: ${RATE_LIMIT_ENABLED:true}
    default:
      limit: 100
      duration: 60
    endpoints:
      login:
        limit: 5
        duration: 300
      register:
        limit: 3
        duration: 3600
      refresh-token:
        limit: 10
        duration: 60

  email:
    enabled: ${EMAIL_ENABLED:true}
    from: ${EMAIL_FROM:noreply@warehouse.com}
    from-name: ${EMAIL_FROM_NAME:Warehouse Management System}
    max-retry: ${EMAIL_MAX_RETRY:3}
    retry-delay-seconds: ${EMAIL_RETRY_DELAY_SECONDS:60}
    async-by-default: ${EMAIL_ASYNC_BY_DEFAULT:true}
    attachment-path: ${EMAIL_ATTACHMENT_PATH:temp/email-attachments}
    max-attachment-size-mb: ${EMAIL_MAX_ATTACHMENT_SIZE_MB:10}
    log-retention-days: ${EMAIL_LOG_RETENTION_DAYS:90}
    queue-name: ${EMAIL_QUEUE_NAME:wms.email.queue}
    exchange-name: ${EMAIL_EXCHANGE_NAME:wms.email.exchange}
    routing-key: ${EMAIL_ROUTING_KEY:wms.email.send}

  otp:
    ttl-minutes: ${OTP_TTL_MINUTES:10}
    resend-limit-seconds: ${OTP_RESEND_LIMIT_SECONDS:60}
    count-ttl-hours: ${OTP_COUNT_TTL_HOURS:24}
    max-send-per-day: ${OTP_MAX_SEND_PER_DAY:10}
